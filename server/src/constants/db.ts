export enum DBTable {
  USERS = "users",
  ANNOUNCEMENTS = "announcements",
}

export enum USERSTableColumns {
  ID = "id",
  USERNAME = "username",
  PASSWORD = "password",
}

export enum ANNOUNCEMENTSTableColumns {
  ID = "id",
  TITLE = "title",
  CONTENT = "content",
  AUTHOR = "author",
}
